<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Grocery.App.ViewModels"
             x:Class="Grocery.App.Views.LoginView"
             x:DataType="vm:LoginViewModel"
             Title="LoginView">
    <StackLayout Padding="20" VerticalOptions="Center" Spacing="10">
        <Label Text="Login" FontSize="24" HorizontalOptions="Center" />
        <Entry Placeholder="Email" Text="{Binding Email}" Keyboard="Email" />
        <Entry Placeholder="Password" Text="{Binding Password}" IsPassword="True" />
        <HorizontalStackLayout HorizontalOptions="Center" Spacing="20">
            <Button Text="Login" Command="{Binding LoginCommand}" />
            <Button Text="Geen account? Ga naar registeren" Command="{Binding GoToRegisterCommand}" />
        </HorizontalStackLayout>
        <Label Text="{Binding LoginMessage}" TextColor="Red" />
    </StackLayout>
</ContentPage>